# PreSketch — エージェント向けの入口

2〜3歳向けプレデッサン（お絵かき）学習アプリ。

**現在の状態**: 要件定義書 第14章の Step 1〜4 相当まで実装済み。レビュー済み。**修正待ち。**

## 最初に読むもの

1. **[`docs/requirements.md`](./docs/requirements.md)** — 仕様の本体。最初から最後まで読むこと
2. **[`docs/fix-instructions-01.md`](./docs/fix-instructions-01.md)** — **いま最初にやる作業。**
   レビューで見つかった修正 A〜G。A から順に、記載順に実施すること

修正 A〜G が完了するまで **Step 5 以降には進まないこと。**

外部の文脈を参照する必要はない。必要な情報はこの2つの文書に揃っている。

## この文書の役割

`docs/requirements.md` は 1300 行ある。読む前に方向を誤らないための要約と、
**絶対に破ってはいけない制約**をここに置く。詳細は必ず本体を参照すること。

## 30秒でわかるプロジェクト

タブレット／スマホのブラウザで動く、指でなぞる1画面のお絵かきゲーム。
点線ガイドを指でなぞると線が引け、7割なぞれば自動で色が付いて紙吹雪が出る。それが12面ある。

- サーバー無し / DB 無し / 通信無し / 画像アセット無し / 音声アセット無し
- 画面は3つ（Home / StageSelect / Play）
- 進捗は localStorage
- **難所は1箇所だけ**: 「指の座標列」と「お手本の座標列」を突き合わせる判定ロジック
  （`docs/requirements.md` 第6章）。ここ以外は素直な React

## 技術スタック

React 19 + TypeScript + Vite + Tailwind CSS + HTML5 Canvas 2D + Web Audio API + canvas-confetti

## 対応環境

| 区分 | 環境 |
|---|---|
| **主対象** | iPad Safari（iPadOS 16+）／ **Android Chrome（Android 10+）** |
| 副対象 | デスクトップ Chrome / Edge / Safari |

**主対象の2つは同等に扱う。**「iPad で動けばよい」という前提を置かないこと。
Android Chrome 固有の問題（プルダウンリロード、アドレスバーによる高さ変動、戻るジェスチャ）は
`docs/requirements.md` 第5.4節・第8.4節に対策がある。

## 絶対に守ること

1. **Capacitor を導入しない。** ネイティブ化は将来の課題。開発環境が Windows で
   JDK / Android SDK / Xcode がすべて無く、iOS ビルドは原理的に不可能
2. **AudioContext は必ずシングルトンにする。** 呼び出しごとに `new AudioContext()` すると
   数秒で上限に達し、以降そのタブは完全に無音になる（第9.1節）
3. **なぞり判定は2段構え。** 画面をランダムに塗りつぶしてクリアできてはいけない。
   順序付きカーソル + Lookahead（第6.3節）**と** on-path 率のゲート（第6.3.1節）の両方が要る。
   順序チェックだけでは直線ステージを守れないことが実測で確認されている
4. **入力は Pointer Events を使う。** Touch Events と `Touch.identifier === 0` の前提は
   iOS で壊れる（第8.2節）
5. **キャンバスは常に正方形にする。** 非正方形だと正規化座標のユークリッド距離が歪み、
   判定領域が楕円になる（決定4）
6. **`100vh` を使わない。`100dvh` を使う。** Android Chrome でアドレスバーの出入りのたびに
   レイアウトが跳ねる（第5.4節）
7. **描画中（ポインタ追跡中）にキャンバスをリサイズしない。** 引いている線が消える（第5.4節）
8. **UA 判定で環境分岐を書かない。** 本書の対策はすべて全環境で有効（第1.4節）
9. **失敗という状態を作らない。** ゲームオーバー、制限時間、減点、否定的な効果音は一切設けない
10. **タップ対象は 64×64px 以上**にする
11. **機能を示す文字を UI に使わない。** アイコンと図形のみ。ひらがなは装飾とモチーフ名のみ

## やらないこと

`docs/requirements.md` 第15章「スコープ外」を必ず読むこと。
PWA 化・多言語・クラウド同期・保護者レポート・BGM 等は**気を利かせて追加しない**。

## 実装前に必ず読む

`docs/requirements.md` **第16章「実装時に踏みやすい落とし穴」**（16項目）。
いずれも実装直後は動いて見えるが、実機で数分使うと破綻するものである。
**13〜16 番は、このリポジトリの実装で実際に踏んだもの**である。

## 完了条件

`docs/requirements.md` 第17章。21項目ある。
**推測で判定せず、実際に実行して確認すること。** 特に以下は見落とされやすい。

- 画面をランダムに塗りつぶしてもクリアできないこと
- なぞり続けても音が途切れないこと（AudioContext のリークがない）
- **Android 実機**での3点（余白のプルダウンでリロードされない／アドレスバーの出入りで線が消えない／
  戻る操作で離脱しない）。これらは**マウスでも iPad でも再現しない**

## 公開

`docs/requirements.md` 第14章 Step 8 に GitHub Pages への公開手順がある（任意）。
`vite.config.ts` の `base` をリポジトリ名と一致させないと画面が真っ白になる点に注意。
