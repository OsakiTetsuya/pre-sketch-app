# PreSketch — エージェント向けの入口

2〜3歳向けプレデッサン（お絵かき）学習アプリ。**実装はまだ始まっていない。** 現在あるのは要件定義書だけ。

## 最初に読むもの

**[`docs/requirements.md`](./docs/requirements.md) を最初から最後まで読むこと。**
実装に必要な情報はすべてそこにある。外部の文脈を参照する必要はない。

## この文書の役割

`docs/requirements.md` は 1100 行ある。読む前に方向を誤らないための要約と、
**絶対に破ってはいけない制約**をここに置く。詳細は必ず本体を参照すること。

## 30秒でわかるプロジェクト

iPad のブラウザで動く、指でなぞる1画面のお絵かきゲーム。
点線ガイドを指でなぞると線が引け、7割なぞれば自動で色が付いて紙吹雪が出る。それが12面ある。

- サーバー無し / DB 無し / 通信無し / 画像アセット無し / 音声アセット無し
- 画面は3つ（Home / StageSelect / Play）
- 進捗は localStorage
- **難所は1箇所だけ**: 「指の座標列」と「お手本の座標列」を突き合わせる判定ロジック
  （`docs/requirements.md` 第6章）。ここ以外は素直な React

## 技術スタック

React 19 + TypeScript + Vite + Tailwind CSS + HTML5 Canvas 2D + Web Audio API + canvas-confetti

## 絶対に守ること

1. **Capacitor を導入しない。** ネイティブ化は将来の課題。開発環境が Windows で
   JDK / Android SDK / Xcode がすべて無く、iOS ビルドは原理的に不可能
2. **AudioContext は必ずシングルトンにする。** 呼び出しごとに `new AudioContext()` すると
   数秒で上限に達し、以降完全に無音になる（第9.1節）
3. **なぞり判定に順序を要求する。** 画面をランダムに塗りつぶしてクリアできてはいけない。
   `advanceTrace`（順序付きカーソル + Lookahead）を使う（第6.3節）
4. **入力は Pointer Events を使う。** Touch Events と `Touch.identifier === 0` の前提は
   iOS で壊れる（第8.2節）
5. **キャンバスは常に正方形にする。** 非正方形だと正規化座標のユークリッド距離が歪み、
   判定領域が楕円になる（決定4）
6. **失敗という状態を作らない。** ゲームオーバー、制限時間、減点、否定的な効果音は一切設けない
7. **タップ対象は 64×64px 以上**にする
8. **機能を示す文字を UI に使わない。** アイコンと図形のみ。ひらがなは装飾とモチーフ名のみ

## やらないこと

`docs/requirements.md` 第15章「スコープ外」を必ず読むこと。
PWA 化・多言語・クラウド同期・保護者レポート・BGM 等は**気を利かせて追加しない**。

## 既知の落とし穴

企画段階の元仕様書には、実装すると確実に発症する不具合が5件あった。
`docs/requirements.md` 第16章に一覧がある。**元仕様書のコード例を参照しないこと。**
本書と `docs/requirements.md` の記述が常に優先される。

## 完了条件

`docs/requirements.md` 第17章。16項目ある。
**推測で判定せず、実際に実行して確認すること。** 特に以下の2つは見落とされやすい。

- 画面をランダムに塗りつぶしてもクリアできないこと
- なぞり続けても音が途切れないこと（AudioContext のリークがない）
